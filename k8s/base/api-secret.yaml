apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: fastapi-demo
type: Opaque
stringData:
  # Exemplo: o Redis deste demo nao exige senha (string vazia).
  # Em producao isto viria de um SealedSecret / External Secrets / SOPS.
  REDIS_PASSWORD: ""
